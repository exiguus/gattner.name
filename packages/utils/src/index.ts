import { isTouch } from './device'
import { Helper, ConsoleColors } from './Helper.class'
import { getRandomInt, sortObject } from './helper'
import { merge } from './merge'
import { isNotEmpty, removeEmpty } from './removeEmpty'
import { isArray, isObject, isError, hasOwnProperty } from './validation'

export {
  isTouch,
  getRandomInt,
  sortObject,
  Helper,
  ConsoleColors,
  merge,
  isNotEmpty,
  removeEmpty,
  hasOwnProperty,
  isError,
  isArray,
  isObject,
}
